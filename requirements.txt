anthropic>=0.40.0
sentence-transformers>=3.0.0
chromadb>=0.5.0
openpyxl>=3.1.0
python-docx>=1.1.0
pdfplumber>=0.11.0
rank-bm25>=0.2.2
pydantic>=2.7.0
click>=8.1.0
streamlit>=1.38.0
# Runtime deps that used to resolve only transitively (C1): src/answer/tokenize.py
# imports these directly, and an upstream change to anthropic/transformers could
# silently break --dry-run with an ImportError otherwise.
httpx>=0.27.0
tokenizers>=0.19.0

fastapi>=0.141.0

sqlcipher3-binary>=0.6.0  # optional at-rest encryption (QRESP_STORE_KEY, pack 3 C11)
